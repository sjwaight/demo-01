apiVersion: placement.kubernetes-fleet.io/v1
kind: ClusterStagedUpdateStrategy
metadata:
  name: sample-app-staged-prod-approval
spec:
  stages:
    - name: development
      labelSelector:
        matchLabels:
          environment: dev
      afterStageTasks:
        - type: TimedWait
          waitTime: 1m
    - name: test
      labelSelector:
        matchLabels:
          environment: test    
      afterStageTasks:
        - type: TimedWait
          waitTime: 1m
      beforeStageTasks:
        - type: Approval
    - name: production 
      labelSelector:
        matchLabels:
          environment: prod
      beforeStageTasks:
        - type: Approval